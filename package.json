{
  "name": "camelscore-models",
  "version": "1.0.0",
  "description": "Create models for fetch with converting under_score style strings to camelCase and the reverse, as well as on objects.",
  "homepage": "https://github.com/grammka/camelscore-models#readme",
  "author": "Pavel Ivanov (grammka@gmail.com)",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/grammka/camelscore-models.git"
  },
  "keywords": [
    "react",
    "redux",
    "action",
    "reducer",
    "creator"
  ],
  "main": "./lib",
  "scripts": {
    "prepublish": "npm run build",
    "clean": "rimraf ./lib",
    "dev": "babel -w ./src --out-dir ./lib",
    "build": "npm run clean && babel ./src --out-dir ./lib"
  },
  "dependencies": {
    "camelscore": "^0.1.2"
  },
  "devDependencies": {
    "babel-plugin-transform-object-rest-spread": "^6.6.5",
    "babel-preset-es2015": "^6.6.0",
    "rimraf": "^2.5.0"
  },
  "directories": {
    "example": "example"
  }
}
